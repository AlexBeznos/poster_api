require 'spec_helper'

describe PosterApi do
  it 'has a version number' do
    expect(PosterApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
